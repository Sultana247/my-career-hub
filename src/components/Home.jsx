import React from 'react';
import Footer from './Footer';
import { Outlet } from 'react-router';

const Home = () => {
    return (
        <div>
            
        </div>
    );
};

export default Home;